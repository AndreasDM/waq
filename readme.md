Bash script for changing wallpaper with a random selected quote.

Edit `folder` and `font` in `quoty.sh`, and run `./quoty.sh`

#### dependencies
* imagemagick
* feh
* jq

#### issues
still some issues with different resolutions, go figure
